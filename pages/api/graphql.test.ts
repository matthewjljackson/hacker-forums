import { schema } from '../../graphql/nexus-schema';
import { context } from '../../graphql/context';

describe('Hacker News api tests', () => {
  it('should check 1 is 1', () => {
    expect(1).toBe(1);
  });
});
